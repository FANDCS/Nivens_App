<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Βασικές άδειες (τεκμηριωμένες ήδη στο ANDROID_SETUP.md αλλά έλειπαν
         από το πραγματικό manifest). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Χρειάζονται για να μπορεί η εφαρμογή να δημιουργεί/γράφει στον
         δημόσιο φάκελο Documents/Nivens (εξαγωγές, εικόνες, σχέδια). -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <application
        android:label="notes_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Στατικό shortcut "Γρήγορη σημείωση" (long-press στο εικονίδιο) -->
            <meta-data
              android:name="android.app.shortcuts"
              android:resource="@xml/shortcuts"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Δέχεται το ACTION_QUICK_NOTE από το shortcut ή το quick-settings tile -->
            <intent-filter>
                <action android:name="com.example.notes_app.ACTION_QUICK_NOTE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

            <!-- ══ Άνοιγμα αρχείων από την εξερεύνηση αρχείων ══════════════
                 Με αυτά τα intent-filters η Nivens εμφανίζεται στη λίστα
                 "Άνοιγμα με..." όταν ο χρήστης πατήσει ένα PDF, ένα Word
                 (.docx) ή ένα αρχείο κειμένου/markdown.
                 Το αρχείο φτάνει στο Flutter μέσω του MethodChannel
                 notes_app/intent (δες MainActivity.kt). -->

            <!-- PDF (content:// και file://) -->
            <intent-filter android:label="Άνοιγμα με Nivens">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content" android:mimeType="application/pdf"/>
                <data android:scheme="file" android:mimeType="application/pdf"/>
            </intent-filter>

            <!-- Word (.docx / .doc) + κείμενο / markdown -->
            <intent-filter android:label="Άνοιγμα με Nivens">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content"
                    android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document"/>
                <data android:scheme="file"
                    android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document"/>
                <data android:scheme="content" android:mimeType="application/msword"/>
                <data android:scheme="content" android:mimeType="text/plain"/>
                <data android:scheme="content" android:mimeType="text/markdown"/>
                <data android:scheme="file" android:mimeType="text/plain"/>
            </intent-filter>

            <!-- Fallback: αρχεία με άγνωστο mime type, μόνο βάσει κατάληξης
                 (πολλοί file managers στέλνουν application/octet-stream). -->
            <intent-filter android:label="Άνοιγμα με Nivens">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content"/>
                <data android:scheme="file"/>
                <data android:host="*"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:pathPattern=".*\\.pdf"/>
                <data android:pathPattern=".*\\.docx"/>
                <data android:pathPattern=".*\\.md"/>
                <data android:pathPattern=".*\\.txt"/>
            </intent-filter>

            <!-- Κοινοποίηση αρχείου προς την εφαρμογή (Share → Nivens) -->
            <intent-filter android:label="Αποστολή στη Nivens">
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/pdf"/>
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Πλακίδιο Γρήγορων Ρυθμίσεων "Γρήγορη σημείωση" -->
        <service
            android:name=".QuickNoteTileService"
            android:label="@string/quick_note_short_label"
            android:icon="@mipmap/ic_launcher"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- permission_handler: επιτρέπει το άνοιγμα της οθόνης ρυθμίσεων
             "Άδεια πρόσβασης σε όλα τα αρχεία" (Android 11+). -->
        <intent>
            <action android:name="android.settings.MANAGE_APP_ALL_FILES_ACCESS_PERMISSION"/>
        </intent>
    </queries>
</manifest>
